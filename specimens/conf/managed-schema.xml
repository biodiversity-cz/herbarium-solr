<?xml version="1.0" encoding="UTF-8" ?>
<schema name="specimens" version="1.7">

    <field name="id" type="string" indexed="true" stored="true" required="true" multiValued="false"/>
    <uniqueKey>id</uniqueKey>

    <!-- text -->
    <field name="title" type="text_general" stored="true"/>
    <field name="herbarium_acronym" type="text_general"/>
    <field name="description" type="text_general"/>
    <field name="locality" type="text_general"/>

    <!-- people -->
    <field name="creator" type="text_general"/>
    <field name="recorded_by" type="text_general"/>

    <!-- date -->
    <fieldType name="pdate" class="solr.DatePointField"/>
    <!-- dates -->
    <field name="event_date" type="pdate" docValues="true"/>
    <field name="created" type="pdate"/>

    <!-- taxonomy -->
    <field name="scientific_name" type="text_general"/>
    <field name="scientific_name_exact" type="string"/>
    <field name="genus" type="string" docValues="true"/>
    <field name="family" type="string" docValues="true"/>
    <field name="specific_epithet" type="string"/>

    <!-- geo -->
    <field name="country" type="string" docValues="true"/>
    <field name="country_code" type="string" docValues="true"/>

    <!-- identifiers -->
    <field name="catalog_number" type="string"/>
    <field name="collection_code" type="strings" multiValued="true" docValues="true"/>

    <!-- misc -->
    <field name="basis_of_record" type="string" docValues="true"/>
    <field name="material_sample_id" type="string"/>

    <field name="previous_identifications" type="text_general" multiValued="true"/>

    <!-- autocomplete -->
    <field name="scientific_name_suggest" type="text_edge"/>
    <field name="collector_suggest" type="text_edge"/>

    <!-- fulltext -->
    <field name="_text_" type="text_general" indexed="true" stored="false" multiValued="true"/>

    <!-- fulltext -->
    <copyField source="title" dest="_text_"/>
    <copyField source="description" dest="_text_"/>
    <copyField source="scientific_name" dest="_text_"/>
    <copyField source="locality" dest="_text_"/>
    <copyField source="creator" dest="_text_"/>
    <copyField source="recorded_by" dest="_text_"/>

    <!-- autocomplete -->
    <copyField source="scientific_name" dest="scientific_name_suggest"/>
    <copyField source="recorded_by" dest="collector_suggest"/>

    <!-- The StrField type is not analyzed, but indexed/stored verbatim. -->
    <fieldType name="string" class="solr.StrField" sortMissingLast="true"/>
    <fieldType name="strings" class="solr.StrField" sortMissingLast="true" multiValued="true"/>

    <!-- since fields of this type are by default not stored or indexed,
       any data added to them will be ignored outright.  -->
    <fieldType name="ignored" stored="false" indexed="false" multiValued="true" docValues="false"
               class="solr.StrField"/>

    <fieldType name="text_general" class="solr.TextField" positionIncrementGap="100" multiValued="true">
        <analyzer type="index">
            <tokenizer name="standard"/>
            <filter name="lowercase"/>
        </analyzer>
        <analyzer type="query">
            <tokenizer name="standard"/>
            <filter name="lowercase"/>
        </analyzer>
    </fieldType>

    <!-- autocomplete -->
    <fieldType name="text_edge" class="solr.TextField">
        <analyzer>
            <tokenizer name="standard"/>
            <filter name="lowercase"/>
            <!--            <filter class="solr.EdgeNGramFilterFactory" minGramSize="2" maxGramSize="20"/>-->
        </analyzer>
    </fieldType>

</schema>