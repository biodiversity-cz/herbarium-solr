<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <luceneMatchVersion>10.4</luceneMatchVersion>

  <updateHandler>
    <autoCommit>
      <maxTime>${solr.autoCommit.maxTime:60000}</maxTime>
      <openSearcher>false</openSearcher>
    </autoCommit>

    <autoSoftCommit>
      <maxTime>${solr.autoSoftCommit.maxTime:3000}</maxTime>
    </autoSoftCommit>
  </updateHandler>

  <requestDispatcher>
    <httpCaching never304="true"/>
  </requestDispatcher>


  <!-- Primary search handler, expected by most clients, examples and UI frameworks -->
  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <int name="rows">10</int>
      <str name="defType">edismax</str>
      <str name="qf">scientific_name^10 genus^6 family^4 title^3 _text_</str>
      <str name="pf">scientific_name^20</str>
      <str name="df">_text_</str>
    </lst>
  </requestHandler>

  <!-- A request handler that returns indented JSON by default -->
  <requestHandler name="/query" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <str name="wt">json</str>
      <str name="indent">true</str>
    </lst>
  </requestHandler>

  <searchComponent name="suggest" class="solr.SuggestComponent">
    <lst name="suggester">
      <str name="name">taxonSuggest</str>
      <str name="lookupImpl">AnalyzingInfixLookupFactory</str>
      <str name="field">scientific_name_suggest</str>
      <str name="suggestAnalyzerFieldType">text_general</str>
      <str name="buildOnStartup">true</str>
    </lst>

    <lst name="suggester">
      <str name="name">creatorSuggest</str>
      <str name="lookupImpl">AnalyzingInfixLookupFactory</str>
      <str name="field">collector_suggest</str>
      <str name="suggestAnalyzerFieldType">text_general</str>
      <str name="buildOnStartup">true</str>
    </lst>
    <lst name="suggester">
      <str name="name">localitySuggest</str>
      <str name="lookupImpl">AnalyzingInfixLookupFactory</str>
      <str name="field">locality_suggest</str>
      <str name="suggestAnalyzerFieldType">text_general</str>
      <str name="buildOnStartup">true</str>
    </lst>
  </searchComponent>

  <requestHandler name="/suggest" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="suggest">true</str>
      <str name="suggest.dictionary">taxonSuggest</str>
      <str name="suggest.dictionary">creatorSuggest</str>
      <str name="suggest.dictionary">localitySuggest</str>
      <str name="suggest.count">10</str>
    </lst>
    <arr name="components">
      <str>suggest</str>
    </arr>
  </requestHandler>

  <!-- Shared parameters for multiple Request Handlers -->
  <initParams path="/update/**,/query,/select,/spell">
    <lst name="defaults">
      <str name="df">_text_</str>
    </lst>
  </initParams>
</config>
